wasm-run:
	RUSTFLAGS=-Awarnings cargo r --message-format short

audio-mock-run:
	RUSTFLAGS=-Awarnings cargo r --message-format short --no-default-features --features=wayland --features=audio

mock-run:
	RUSTFLAGS=-Awarnings cargo r --message-format short --no-default-features --features wayland

