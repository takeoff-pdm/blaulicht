[package]
name = "blaulicht-sample-animation-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
blaulicht-shared = { path = "../../shared" }
blaulicht-plugin-framework = { path = "../../plugin_framework" }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

